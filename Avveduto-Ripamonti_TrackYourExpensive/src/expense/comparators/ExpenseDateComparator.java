package expense.comparators;

import java.util.Comparator;

public class ExpenseDateComparator implements Comparator<ExpenseDateComparator> {
    @Override
    public int compare(ExpenseDateComparator expenseDateComparator, ExpenseDateComparator t1) {
        return 0;
    }
}
